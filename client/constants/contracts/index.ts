import { Address } from "viem";
// import TASABI from "../abi/TASABI.json";

export const CONTRACTS = {
  fundTheCommons: {
    5: {
      contract: "" as Address,
      abi: "TASABI",
    },
  },
};
